# Stage 3 — Buyer Auth Foundation

This stage added buyer-facing authentication screens. The current Stage 6 flow keeps `/login` and `/register` buyer-only.

Current dashboard access is not handled through public login. It is accessed from the hidden dashboard icon on the profile card, then protected by the private owner gate and notification email step.
